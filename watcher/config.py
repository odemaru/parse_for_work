"""Что ищем и где."""

ROLE_ROOTS = ("analitik", "analyst", "analytic", "analitic")

DOMAIN_ROOTS = (
    "produkt",   # продуктовый аналитик, продуктовая аналитика, product analyst
    "product",
    "dann",      # аналитик данных, данных/данными
    "data",
)

# Слова, из-за которых вакансия отсеивается, даже если корни выше совпали:
# соседние специальности и пищевое "продуктовое качество" в рознице.
EXCLUDE_ROOTS = (
    "sistemn",
    "system",
    "biznes",
    "business",
    "finansov",
    "marketing",
    "videoanalitik",
    "kachestv",
    "pishev",
    "1s",
    "1c",
)

# Источники, которые собираются каждый запуск.
ENABLED_SOURCES = (
    "wildberries",
    "x5",
    "mts",
    "magnit",
    "tbank",
    "lamoda",
    "severstal",
)

# Компании, до которых дотянуться не вышло. Когда доступ появится,
# останется дописать модуль в watcher/sources/ и добавить его имя
# в ENABLED_SOURCES.
DISABLED_SOURCES = {
    "Ozon": "карьерный сайт закрыт заглушкой, каталога вакансий нет",
    "Альфа-Банк": "сайт не отвечает за пределами России",
}

# Города Магнита: его API требует locality_id и без него отдаёт пустой список.
MAGNIT_LOCALITIES = {
    "Москва": 3047,
    "Краснодар": 506,
    "Санкт-Петербург": 3078,
}

# Направления Магнита, внутри которых вообще имеет смысл искать аналитиков —
# иначе пришлось бы выкачивать десятки тысяч вакансий продавцов.
MAGNIT_DIRECTIONS = ("Анализ и бизнес-анализ", "ИТ")

# Верхняя граница требуемого опыта: вакансия проходит, если её нижняя
# планка не выше этого числа лет. "Стажёр" и "junior" проходят всегда,
# "senior", "тимлид" и "руководитель" отсекаются по названию.
MAX_EXPERIENCE_YEARS = 3

# Т-Банк, Lamoda и Магнит опыт не публикуют. True — показывать такие
# вакансии, отсеивая лишь по грейду в названии.
KEEP_UNKNOWN_EXPERIENCE = True

# Гифка, которая уходит в чат, когда новых вакансий нет.
# Путь считается от корня репозитория; если файла нет, шлётся обычный текст.
NOTHING_FOUND_ANIMATION = "assets/nothing-found.mp4"

REQUEST_TIMEOUT = 30
STATE_TTL_DAYS = 180
